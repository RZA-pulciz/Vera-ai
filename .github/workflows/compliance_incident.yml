name: Compliance & Incident Nightly
on: { schedule: [{ cron: "30 2 * * *" }] }
jobs:
  compliance: { runs-on: ubuntu-latest, steps: [ { uses: actions/checkout@v4 }, { run: echo "Aggregazione compliance" } ] }
  incident:   { runs-on: ubuntu-latest, steps: [ { uses: actions/checkout@v4 }, { run: echo "Riepilogo incidenti" } ] }
