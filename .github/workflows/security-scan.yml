name: Security Scan
on: { schedule: [{ cron: "0 3 * * *" }] }
jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: pip install bandit && bandit -r orchestrator services superior_agent || true
